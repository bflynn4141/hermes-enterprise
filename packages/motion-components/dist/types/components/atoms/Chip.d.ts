/** Monospace token chip — for code values like `updated_at`. */
export declare function Chip({ children, tone, className, }: {
    children: React.ReactNode;
    tone?: "neutral" | "accent" | "orange";
    className?: string;
}): import("react").JSX.Element;
