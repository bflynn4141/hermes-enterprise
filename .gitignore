node_modules/
.wrangler/
dist/
!apps/client/dist/
!apps/client/dist/**
!packages/motion-components/dist/
!packages/motion-components/dist/**
!runtime/hermes/enterprise_bridge/dashboard/dist/
!runtime/hermes/enterprise_bridge/dashboard/dist/**
coverage/
*.tsbuildinfo

# Project-local worktrees and tool state. These directories can contain nested
# repositories, dependency trees, or credentials and must never be staged.
/work/
/~/
/.agentcash/

# Playwright screenshots are generated evidence. CI retains them as artifacts;
# only the curated public images under docs/assets belong in Git.
/apps/client/qa/

# Local secrets and local environment. The .example files beside them are
# committed and are the documentation of what a deployment needs.
.dev.vars
.env
.env.*
!.env.example
!.dev.vars.example
*.env

# The generated test-run variables (apps/client/scripts/e2e-live.mjs).
.dev.vars.*
!.dev.vars.example
